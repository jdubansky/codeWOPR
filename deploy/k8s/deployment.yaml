apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: codewopr-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: codewopr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codewopr
  template:
    metadata:
      labels:
        app: codewopr
    spec:
      containers:
        - name: manager
          image: codewopr:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: openai-api
                  key: api-key
            - name: DATABASE_URL
              value: sqlite:////data/codewopr.db
            - name: PORT
              value: "3000"
          volumeMounts:
            - mountPath: /data
              name: data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: codewopr-data
---
apiVersion: v1
kind: Service
metadata:
  name: codewopr
spec:
  selector:
    app: codewopr
  ports:
    - name: http
      port: 80
      targetPort: 3000

